db.sqlite3

# User uploads — not version controlled (kept on each server, breaks pulls if tracked)
/media/

# Django-collected static (regenerated by `collectstatic`); source app static stays tracked
/static/admin/

# Python bytecode
__pycache__/
*.pyc
